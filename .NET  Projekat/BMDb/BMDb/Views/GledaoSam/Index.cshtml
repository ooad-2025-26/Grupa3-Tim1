
@model IReadOnlyList<BMDb.ViewModels.ProfileMediaItemViewModel>
@using Microsoft.AspNetCore.Identity
@inject BMDb.Services.IMediaImageService MediaImageService
@inject UserManager<Osoba> UserManager
@{
    var currentUser = await UserManager.GetUserAsync(User);
    var avatarUrl = string.IsNullOrWhiteSpace(currentUser?.Avatar)
        ? "/images/uploads/user-img.png"
        : MediaImageService.ResolvePosterUrl(currentUser.Avatar);
}

    <div class="hero user-hero">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="hero-ct">
                        <h1 style="position: relative; left: -18px;">Filmovi koje sam gledao</h1>
                        <ul class="breadcumb">
                            <li class="active"><a asp-controller="Home" asp-action="Glavna">Početna</a></li>
                            <li><span class="ion-ios-arrow-right"></span> Gledao sam</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="page-single userfav_list">
        <div class="container">
            <div class="row ipad-width2">
                <div class="col-md-3 col-sm-12 col-xs-12">
                    <div class="user-information">
                        <div class="user-img">
                            <a asp-controller="Osoba" asp-action="Details"><img class="bmd-avatar" src="@avatarUrl" alt=""><br></a>
                            <a asp-controller="Osoba" asp-action="Details" class="redbtn">Promijeni avatar</a>
                        </div>
                        <div class="user-fav">
                            <p>Detalji računa</p>
                            <ul>
                                <li><a asp-controller="Osoba" asp-action="Details">Profil</a></li>
                                <li><a asp-controller="GledatCu" asp-action="Index">Gledat ću</a></li>
                                <li class="active"><a asp-controller="GledaoSam" asp-action="Index">Gledao sam</a></li>
                                <li><a asp-controller="Recenzija" asp-action="Index">Recenzije</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
                <div class="col-md-9 col-sm-12 col-xs-12">
                    <div class="topbar-filter user">
                        <p>Pronađeno: <span>@Model.Count naslova</span></p>
                        <label>Sortiraj po:</label>
                        <select>
                            <option value="range">Najnovije dodano</option>
                            <option value="saab">Ocjena (najviša)</option>
                        </select>
                    </div>

                    <div class="flex-wrap-movielist user-fav-list">
                        @foreach (var item in Model)
                        {
                            <div class="movie-item-style-2">
                                <a class="bmd-poster-link" asp-controller="@item.ControllerName" asp-action="Details" asp-route-id="@item.EntertainmentId">
                                    <img src="@MediaImageService.ResolvePosterUrl(item.PosterLink)" alt="">
                                </a>
                                <div class="mv-item-infor">
                                    <h6><a asp-controller="@item.ControllerName" asp-action="Details" asp-route-id="@item.EntertainmentId">@item.Naziv <span>(@item.GodinaIzlaska)</span></a></h6>
                                    <p class="rate"><i class="ion-android-star"></i><span>@item.ProsjecnaOcjena.ToString("0.0")</span> /10</p>
                                    <p class="describe">@item.Opis</p>
                                    <p class="run-time">Trajanje: @item.Trajanje min . <span>Premijera: @item.GodinaIzlaska</span></p>
                                    <p>Režiser: @item.Reditelj</p>
                                </div>
                            </div>
                        }
                    </div>

                    <div class="topbar-filter">
                        <label>Filmova po stranici:</label>
                        <select>
                            <option value="range">@Model.Count naslova</option>
                        </select>
                        <div class="pagination2">
                            <span>Stranica 1 od 1:</span>
                            <a class="active" asp-controller="GledaoSam" asp-action="Index">1</a>
                            <a asp-controller="GledaoSam" asp-action="Index"><i class="ion-arrow-right-b"></i></a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
