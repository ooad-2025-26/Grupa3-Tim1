@model IEnumerable<BMDb.Models.EntertainmentZanr>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EntertainmentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ZanrId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EntertainmentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ZanrId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDVeze">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDVeze">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDVeze">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
